
<%= render "categories/header" %>
<h1>Categories</h1>

<div id="categories">
  <% @categories.each do |category| %>
    <%= render category %>

    Total amount for this categories is 
    <%= category.transactions.sum(:amount) %>

    <p>
      
      <%= link_to "see transactions", category_transactions_path(category) %>
    </p>
  <% end %>
</div>

<%= link_to "New category", new_category_path %>
