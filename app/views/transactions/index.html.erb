total amount is 
 <%= @transactions.sum(:amount) %>

<table>
  <tr>
    <th>Name</th>
    <th>Amount</th>
    <th>Category</th>
    <th>User</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @transactions.each do |transaction| %>
  <tr>
    <td><%= transaction.name %></td>
    <td><%= transaction.amount %></td>
    <td><%= transaction.transaction_category %></td>
    <td><%= link_to 'Show', [transaction.category, transaction] %></td>
    <td><%= link_to 'Edit', edit_category_transaction_path(transaction.category, transaction) %></td>
    <td><%= button_to 'Destroy', [transaction.category, transaction], method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Transaction', new_category_transaction_path(params[:category_id]) %>
<%= link_to 'Back', categories_path %>